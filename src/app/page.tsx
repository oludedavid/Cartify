import SigninButton from "@/components/auth/SigninButton";
import { NavBar } from "@/components/ui/nav-bar";

export default function Home() {
  return (
    <div className="font-[family-name:var(--font-geist-sans)] flex w-full justify-center">
      <p className="text-3xl font-bold">Add ur products to the Cart!</p>
    </div>
  );
}
